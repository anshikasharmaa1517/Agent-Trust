# Razorpay Agent Trust Gateway — Environment Configuration

# Razorpay Test Mode (optional — falls back to simulated orders)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Anthropic Claude AI (optional — falls back to regex parser + template explanations)
ANTHROPIC_API_KEY=
